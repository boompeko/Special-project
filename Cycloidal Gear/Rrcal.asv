function [Rr] = Rrcal(N,R,E,Rr,t)


phi = atan(sin((1-N)*t)/((R/(E*N))-cos((1-N)*t)));

Cf = [R-Rr*cos(phi);Rr*sin(phi);1];

dx = diff(Cf(1),t);
d2x = diff(dx,t);

dy = diff(Cf(2),t);
d2y = diff(dy,t);

disp(dx);
disp(dy);
disp(d2x);
disp(d2y);